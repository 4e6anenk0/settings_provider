name: settings_provider
description: A library for providing declarative configuration of app settings (wraper for shared_preference).
version: 0.1.1
homepage: https://github.com/4e6anenk0/settings_provider/tree/main
repository: https://github.com/4e6anenk0/settings_provider/tree/main
issue_tracker: https://github.com/4e6anenk0/settings_provider/issues
topics: [settings, settings-management]

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  nested: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

screenshots:
  - description: The settings_provider package logo.
    path: img/logo-2.png